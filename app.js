"use strict";
(function () {
    var getedad = function () {
        return 100 - 70;
    };
    var nombre = 'Amnhed';
    var apelllido = 'Lagunas';
    var salida = nombre + " " + apelllido + " ( " + getedad() + ")";
    console.log(salida);
})();
